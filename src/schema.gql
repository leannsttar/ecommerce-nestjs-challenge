# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  id: ID!
  name: String!
  products: [Product!]
}

input CreateCategoryInput {
  name: String!
}

input CreateProductInput {
  categoryId: ID!
  description: String!
  images: [String!]
  name: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Image {
  id: ID!
  isMain: Boolean!
  url: String!
}

type Mutation {
  createCategory(input: CreateCategoryInput!): Category!
  createProduct(input: CreateProductInput!): ProductAdmin!
  deleteCategory(id: ID!): ID!
  deleteProduct(id: ID!): ID!
  disableProduct(id: ID!): ProductAdmin!
  enableProduct(id: ID!): ProductAdmin!
  updateCategory(id: ID!, input: UpdateCategoryInput!): Category!
  updateProduct(id: ID!, input: UpdateProductInput!): ProductAdmin!
}

type Product implements ProductBase {
  category: Category!
  description: String!
  featuredImage: Image
  id: ID!
  images: [Image!]!
  name: String!
}

type ProductAdmin implements ProductBase {
  category: Category!
  createdAt: DateTime!
  deletedAt: DateTime
  description: String!
  featuredImage: Image
  id: ID!
  images: [Image!]!
  isActive: Boolean!
  name: String!
  updatedAt: DateTime!
}

interface ProductBase {
  category: Category!
  description: String!
  featuredImage: Image
  id: ID!
  images: [Image!]!
  name: String!
}

type Query {
  allProducts: [ProductAdmin!]!
  categories: [Category!]!
  category(id: ID!): Category
  product(id: ID!): Product
  productManager(id: ID!): ProductAdmin
  products: [Product!]!
}

input UpdateCategoryInput {
  name: String
}

input UpdateProductInput {
  categoryId: ID
  description: String
  name: String
}